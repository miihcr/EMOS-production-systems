



other.rev >= 0
turnover >= 0
staff >= 1

do_by(prob, by=size, fun=sum, na.rm=TRUE) == 1

turnover + other.rev == total.rev

if (staff >= 1) staff.costs >= 1

mean(profit, na.rm = TRUE) >= 100





